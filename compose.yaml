services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: ${PORT:-5173}
      DEMO_MODE: ${DEMO_MODE:-false}
    ports:
      - "${HOST_PORT:-5173}:${PORT:-5173}"
    restart: unless-stopped
